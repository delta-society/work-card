---
name: day3-handoff
description: 3회차 — 첫 산출물을 실제 업무에 쓸 수 있는 상태로 정리하고, 혼자 못 푼 것을 인계 가능한 형태로 남긴다. "3회차", "Day 3", "마무리" 요청에 사용.
---

# Day 3: 첫 산출물 정리 + 혼자 못 푼 것 인계

3회 프로그램의 마지막이다. 발표 자료 27장의 약속 — "수료가 아니라 시작"을 실제로 만드는 회차.

---

## STOP PROTOCOL — 절대 위반 금지

> 이 프로토콜이 이 스킬의 최우선 규칙이다. 위반하면 수업이 망가진다.

```
┌─ Phase A (한 턴) ───────────────────────────────────────┐
│ 1. references/ 에서 해당 블록 파일의 EXPLAIN 섹션을 읽는다 │
│ 2. 설명한다                                              │
│ 3. 같은 파일의 EXECUTE 섹션을 읽는다                      │
│ 4. "지금 직접 해보세요"라고 안내한다                       │
│ 5. ⛔ 여기서 반드시 STOP. 턴을 종료한다.                   │
│                                                          │
│ ❌ 절대 하지 않는 것: 확인 질문 출제, CHECK 섹션 읽기       │
│ ❌ 절대 하지 않는 것: AskUserQuestion 호출                 │
│ ❌ 절대 하지 않는 것: "해보셨나요?" 묻기                    │
└──────────────────────────────────────────────────────────┘

  ⬇️ 사용자가 "했어", "완료", "다음"이라고 입력한다

┌─ Phase B (다음 턴) ─────────────────────────────────────┐
│ 1. 같은 파일의 CHECK 섹션을 읽는다                        │
│ 2. AskUserQuestion으로 확인한다                          │
│ 3. 피드백을 준다                                          │
│ 4. 다음 블록으로 갈지 AskUserQuestion으로 묻는다           │
│ 5. ⛔ 다음 블록은 다시 Phase A부터.                        │
└──────────────────────────────────────────────────────────┘
```

### 핵심 금지 사항

1. **Phase A에서 AskUserQuestion을 호출하지 않는다**
2. **Phase A에서 CHECK 섹션을 읽지 않는다**
3. **한 턴에 EXPLAIN + CHECK를 같이 하지 않는다** — 반드시 2턴으로 나눈다
4. **한 번에 한 블록만 진행한다** — references 파일을 미리 여러 개 읽지 않는다

### Phase A 종료 문구

Phase A 마지막에는 반드시 아래를 출력하고 STOP한다.

```
---
👆 위 내용을 직접 해보세요.
끝나면 "완료" 또는 "다음"이라고 입력해주세요.
```

이 문구 뒤에 어떤 도구 호출이나 추가 텍스트도 출력하지 않는다.

### 발표 자료와의 관계

이 스킬은 강사가 화면에 띄우는 발표 자료의 **실습 파트를 그대로 실행**한다. 자료에 없는 내용을 새로 만들지 않고, 자료가 다루지 않은 회차 내용을 미리 당겨오지 않는다.

---

## 시작 전 — 1·2회차 산출물 읽기

`work-cards/` 카드, `CLAUDE.md`, `.claude/skills/` 아래 본인 스킬을 모두 읽는다.

- 카드에 **Day 2 세 줄이 있으면**: 요약해 보여주고 Block 0으로.
- Day 1만 있고 **Day 2가 없으면**: `/day2-measure`를 먼저 하라고 안내하고 종료한다. **판정 기준 없이 정리하면 무엇을 완성으로 볼지가 없다.**
- 카드가 **없으면**: `/day1-start`부터 안내하고 종료한다.

---

## References 파일 맵

| 블록 | 파일 | 주제 |
|------|------|------|
| 0 | `references/block0-usage.md` | 이번 주에 실제로 썼나 |
| 1 | `references/block1-polish.md` | 산출물을 쓸 수 있는 상태로 |
| 2 | `references/block2-share.md` | 남이 쓸 수 있게 만들기 |
| 3 | `references/block3-handoff.md` | **혼자 못 푼 것 인계 (핵심)** |
| 4 | `references/block4-close.md` | 3회 돌아보기 + 다음 |

---

## 오늘 끝나면 손에 남는 것

- 실제 업무에 쓸 수 있는 레시피 하나
- 남이 받아 쓸 수 있게 정리된 상태
- **인계 목록** — 개인이 못 넘는 벽을 성격별로 갈라 적은 것

## 완료 기준

셋 다 돼야 끝이다.

1. 레시피를 실제 업무 건으로 한 번 돌렸다
2. 나온 결과를 2회차 기준으로 판정했다
3. 인계 목록을 적었다

**만들고 끝내지 않는다** — 실제 업무 건으로 돌아가는 걸 봐야 자기 것이 된다.

---

## 시작

1·2회차 산출물을 확인시킨 뒤 Block 0의 Phase A부터 진행한다.
