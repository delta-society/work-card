---
name: day3-build
description: 3회차 — 2회차에 완성한 카드를 실제로 돌아가는 나만의 스킬로 만들고, 돌려보고, 고친다. "3회차", "Day 3", "스킬 만들기" 요청에 사용.
---

# Day 3: 내 스킬 만들기

이 스킬이 호출되면 아래 **STOP PROTOCOL**을 반드시 따른다.

---

## STOP PROTOCOL — 절대 위반 금지

> 이 프로토콜이 이 스킬의 최우선 규칙이다. 위반하면 수업이 망가진다.

```
┌─ Phase A (한 턴) ───────────────────────────────────────┐
│ 1. references/ 에서 해당 블록 파일의 EXPLAIN 섹션을 읽는다 │
│ 2. 설명한다                                              │
│ 3. 같은 파일의 EXECUTE 섹션을 읽는다                      │
│ 4. "지금 직접 해보세요"라고 안내한다                       │
│ 5. ⛔ 여기서 반드시 STOP. 턴을 종료한다.                   │
│                                                          │
│ ❌ 절대 하지 않는 것: 확인 질문 출제, CHECK 섹션 읽기       │
│ ❌ 절대 하지 않는 것: AskUserQuestion 호출                 │
│ ❌ 절대 하지 않는 것: "해보셨나요?" 묻기                    │
└──────────────────────────────────────────────────────────┘

  ⬇️ 사용자가 "했어", "완료", "다음"이라고 입력한다

┌─ Phase B (다음 턴) ─────────────────────────────────────┐
│ 1. 같은 파일의 CHECK 섹션을 읽는다                        │
│ 2. AskUserQuestion으로 확인한다                          │
│ 3. 피드백을 준다                                          │
│ 4. 다음 블록으로 갈지 AskUserQuestion으로 묻는다           │
│ 5. ⛔ 다음 블록은 다시 Phase A부터.                        │
└──────────────────────────────────────────────────────────┘
```

### 핵심 금지 사항

1. **Phase A에서 AskUserQuestion을 호출하지 않는다**
2. **Phase A에서 CHECK 섹션을 읽지 않는다**
3. **한 턴에 EXPLAIN + CHECK를 같이 하지 않는다** — 반드시 2턴으로 나눈다
4. **한 번에 한 블록만 진행한다** — references 파일을 미리 여러 개 읽지 않는다

### Phase A 종료 문구

Phase A 마지막에는 반드시 아래를 출력하고 STOP한다.

```
---
👆 위 내용을 직접 해보세요.
끝나면 "완료" 또는 "다음"이라고 입력해주세요.
```

이 문구 뒤에 어떤 도구 호출이나 추가 텍스트도 출력하지 않는다.

---

## 시작 전 확인

`work-cards/` 폴더에서 카드를 찾아 읽는다.

- 여섯 칸이 **다 차 있으면**: 카드를 보여주고 Block 0으로.
- **순서·판단 지점이 비어 있으면**: `/day2-map`을 먼저 하라고 안내하고 종료한다. **판단 지점 없이 만든 스킬은 사람이 정해야 할 것까지 AI가 정해버린다** — 이게 이 프로그램에서 가장 피해야 할 결과다.
- 카드가 **없으면**: `/day1-start`부터 안내하고 종료한다.

---

## References 파일 맵

| 블록 | 파일 | 주제 |
|------|------|------|
| 0 | `references/block0-anatomy.md` | 스킬이 뭘로 되어 있나 |
| 1 | `references/block1-generate.md` | **내 카드를 스킬로** |
| 2 | `references/block2-run.md` | 돌려보기 — 첫 판은 어긋난다 |
| 3 | `references/block3-fix.md` | 말로 고치기 |
| 4 | `references/block4-wrap.md` | 남은 것 정리 |

> 각 파일은 `## EXPLAIN`, `## EXECUTE`, `## CHECK` 섹션으로 되어 있다.

---

## 오늘 끝나면 손에 남는 것

- `.claude/skills/<본인업무>/SKILL.md` — 실제로 돌아가는 나만의 스킬
- 최소 1회 실행 + 최소 1회 수정 경험
- 혼자 못 푼 것 1개 (적어둔 상태)

## 완료 기준

세 개가 다 돼야 끝이다.

1. 스킬 파일이 생겼다
2. 돌려봤다
3. 한 번 고쳐봤다

**만들고 끝내지 않는다** — 돌아가는 걸 봐야 자기 것이 된다.

---

## 시작

카드를 확인시킨 뒤 Block 0의 Phase A부터 진행한다.
